# covid-backend

Install via

```bash
pip install -r requirements.txt
```

Run

```bash
python papers.py
```

Test

```bash
http --session-read-only=./session localhost:8000/papers q=="whats corona"
```
